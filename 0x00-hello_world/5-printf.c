#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Prints text with the printf function.
 * Return: returns 0 upon successful completion
 */

int main (void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
